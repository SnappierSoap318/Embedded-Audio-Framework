# Physical WROOM Wi-Fi / LMS connection — 2026-09-12

Result: flash verification, Wi-Fi association, DHCP and LMS registration **PASS**.
Audible output, pause/volume operation and sustained playback were not tested in
this session. The I2S-capable image is installed; no playback command was sent.

Board: ESP32-D0WD-V3 revision 3.1, MAC `aa:bb:cc:dd:ee:ff`, 40 MHz crystal,
4 MB flash. Connected through `/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0`.
The application is `platform/esp32_lms`, with PSRAM/Bluetooth disabled and the
existing GPIO26/25/27 I2S assignment. Private credentials remain in the ignored
local header and generated firmware, never in this record.

The first flashed image booted but timed out waiting for Wi-Fi. The upstream
Espressif driver default enabled automatic DHCP alongside application-managed
DHCP. The board configuration now explicitly disables driver-owned DHCP and
reconnect; the application owns both. Association and timeout diagnostics were
added to distinguish Wi-Fi association from IPv4 acquisition. A subsequent build,
flash and reset connected successfully. This single successful attempt does not
prove the cause of the initial timeout or qualify repeated reconnection.

Final image SHA-256:
`707dc7eddcdd4d8b188f88e2559a52fa82eb9ae6ed0a3eaf961890b8663aa345`.
Esptool wrote at `0x1000` and reported `Hash of data verified`.
Serial reset capture at 115200 baud, 40 seconds:

```text
I (101) boot: libc heap size 44 kB.
*** Booting Zephyr OS build v4.3.0 ***
EAF LMS board: player aa:bb:cc:dd:ee:ff, server 192.168.11.132:3483
Wi-Fi association result: 0
Wi-Fi IPv4: 192.168.8.176
LMS connected; select this player's MAC in the server UI
```

A read-only LMS `players` query confirmed `connected: 1`, model `eaf`, player
MAC `aa:bb:cc:dd:ee:ff`, and displayed name **SqueezePlay 2**. The server reported
a NAT/container peer address; use the DHCP address above for the board itself.
DHCP addresses may change. No browser/logging endpoint is installed on the board.

Local capture: `build-wroom-lms/boot-serial.log` (ignored). The real Xtensa image
built successfully; the updated network controller also built under native_sim
and passed `tools/check_code.py build-lms-sim` (17 translation units, zero failed
checks). The simulator does not validate radio behavior.

Next: wire/power verification, low-volume channel tests, sustained playback,
AP/server restart, and measured heap/stack/timing headroom. Remote logging over
Wi-Fi is a separate pending feature; it cannot deliver logs while Wi-Fi is down.
