# WROOM has no external RAM. The upstream DevKitC template names a WROVER.
CONFIG_ESP_SPIRAM=n
CONFIG_ESP_SIMPLE_BOOT=y
CONFIG_ESPTOOLPY_FLASHSIZE_4MB=y
CONFIG_ESPTOOLPY_FLASHMODE_DIO=y
CONFIG_ESPTOOLPY_FLASHFREQ_40M=y
CONFIG_ESP32_WIFI_STATIC_RX_BUFFER_NUM=4
CONFIG_ESP32_WIFI_DYNAMIC_RX_BUFFER_NUM=8
CONFIG_ESP32_WIFI_DYNAMIC_TX_BUFFER_NUM=8

# The application owns DHCP and retry timing.
CONFIG_ESP32_WIFI_STA_AUTO_DHCPV4=n
CONFIG_ESP32_WIFI_STA_RECONNECT=n

# TCP receive window must be backed by enough RX buffers. The upstream 8 KiB
# window holds only ~46 ms of 44.1 kHz/16-bit stereo, capping HTTP intake near
# window/RTT (~80 KB/s at the observed ~100 ms RTT) below the 176 KB/s needed.
# Phase 1a: double the window and grow the RX pool to back it.
CONFIG_NET_TCP_MAX_RECV_WINDOW_SIZE=16384
CONFIG_NET_BUF_RX_COUNT=96
CONFIG_NET_PKT_RX_COUNT=24

# Keep draining the socket while the output reservoir is full so the server's
# TCP window stays open. Source PCM staging in internal RAM (no PSRAM here).
CONFIG_EAF_LMS_INGRESS_BYTES=16384

# Zephyr 4.3's ESP32 Wi-Fi driver depends on !SMP, so dual-core pinning is not
# available with networking. Keep audio above network work on the single core:
# the TCP worker default (2) outranks EAF audio (3); move it below.
CONFIG_NET_TCP_WORKER_PRIO=5
